let config = (function(){
	let configSevice = {
		'1' :  array => array.sort(service.compare),
		'2' :  array => array.sort(service.compareReverse),
		'3' :  array => array.sort(service.compareDate),
		'4' :  array => array.sort(service.compareDateReverse),
	}

	return {
		configSevice
	}
})(); 